import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
export const logo = {
    fontSize: "20px",
    fontWeight: "bold",
    cursor: "pointer",
    textDecoration: "none",
};
